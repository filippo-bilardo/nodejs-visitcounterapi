# =============================================================================
# Visit Counter API - Environment Configuration Example
# =============================================================================
# Copia questo file come .env nella stessa directory e modifica i valori

# Configurazione base
NODE_ENV=production
PORT=3000
DB_PATH=/app/data/visits.db

# Sicurezza - IMPORTANTE: Cambia questo valore in produzione!
# Genera un salt sicuro con: openssl rand -hex 32
SALT=your-secure-random-salt-here-change-in-production

# Timezone
TZ=Europe/Rome

# CORS Configuration (opzionale)
# CORS_ORIGIN=https://yourdomain.com,https://www.yourdomain.com
# CORS_ORIGIN=* (per permettere tutti i domini - non consigliato in produzione)

# Rate Limiting (opzionale)
# RATE_LIMIT_REQUESTS=60
# RATE_LIMIT_WINDOW=60

# Admin Token (opzionale - per API amministrative)
# ADMIN_TOKEN=your-admin-token-here

# Database Configuration (opzionale - per database esterni)
# DATABASE_TYPE=sqlite (default) | postgres | mysql
# DATABASE_HOST=localhost
# DATABASE_PORT=5432
# DATABASE_NAME=visit_counter
# DATABASE_USER=username
# DATABASE_PASS=password

# Redis Configuration (opzionale - per cache e rate limiting)
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_PASSWORD=

# Logging Level
# LOG_LEVEL=info (default) | debug | warn | error

# Backup Configuration (opzionale)
# BACKUP_ENABLED=true
# BACKUP_INTERVAL=24h
# BACKUP_RETENTION=7d
